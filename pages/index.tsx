import type { NextPage, GetStaticProps } from 'next'
import Head from 'next/head'

import { serverSideTranslations } from "next-i18next/serverSideTranslations"
import { useTranslation } from "next-i18next";

import { useRouter, NextRouter } from "next/router";
import Link from "next/link";

const Home: NextPage = () => {
  const { t } = useTranslation("firstPage");
  const router: NextRouter = useRouter();

  // const keyboardEvents = (e: React.KeyboardEvent) => {
  //   console.log("key pressed");
  //   if (e.shiftKey){
  //     switch (e.key){
  //       case "ArrowUp": console.log("shift up"); break;
  //       case "ArrowDown": console.log("shift down"); break;
  //     }
  //   }
  // }

  return (
    <div className="" >
      <Head>
        <title>Horizon - Paper Journey</title>
        <meta name="description" content="Generated by create next app" />

        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className="bg-red-300 w-full h-40 absolute bg-opacity-50">
        <div className="bg-blue-300">
          <Link href="/" locale="en"><a><button>To English locale</button></a></Link>
          <Link href="/" locale="de"><a><button>To German locale</button></a></Link>
          <Link href="/" locale="fr"><a><button>To French locale</button></a></Link>
          <Link href="/" locale="ja"><a><button>To Japanese locale</button></a></Link>

          <h1>change locale</h1>
          <h1>testing sentence: {t("testing")}</h1>
          <h1>current locale: {router.locale}</h1>
        </div>
      </div>

      <main className="flex-col scroll-snap-y-madatory overflow-y-scroll h-screen max-h-screen smooth-scroll-y">
        <div className="w-screen h-screen bg-red-300 scroll-snap-start">
          <h1>Weclome</h1>
        </div>
        <div className="w-screen h-screen bg-green-300 scroll-snap-start">

        </div>
        <div className="w-screen h-screen bg-purple-300 scroll-snap-start">

        </div>
        <div className="w-screen h-screen bg-indigo-700 scroll-snap-start">

        </div>
        <div className="w-screen h-screen bg-pink-300 scroll-snap-start">

        </div>
      </main>

    </div>
  )
}

export const getStaticProps: GetStaticProps = async ({ locale }) => ({
  props: {
    ...await serverSideTranslations(locale!, ["common", "firstPage"]),
  }
})

export default Home
